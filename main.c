#include <atmel_start.h>

int main(void)
{
	/* Initializes MCU, drivers and middleware */
	//atmel_start_init();

	/* Replace with your application code */
    volatile int x = 0;
	while (1) {
        x++;
	}
}
